# Agent Instructions

## Cursor Cloud specific instructions

- This repository currently has no dependency manifests, setup scripts, active Git hooks, or runnable service definitions. The root `README.md` is blank.
- The startup dependency refresh is intentionally a no-op until application code or dependency manifests are added.
- There are currently no lint, test, build, dev-server, or end-to-end "hello world" commands to run. Re-check the repository structure before assuming a service exists.
